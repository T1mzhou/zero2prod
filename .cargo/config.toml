# 在 Linux 系统上
# - Ubuntu 系统，`sudo apt-get install lld clang`
# - Arch 系统，`sudo pacman -S lld clang`
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "linker=clang", "-C", "link-arg=-fuse-ld=lld"]
